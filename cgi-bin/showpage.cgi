#!C:\Perl\bin\perl.exe

use CGI qw(:standard);
#use ftowa_toolkit;
use ptHTML;
use ptDIO;

$|      = 1;
&ptHTML::startHTML();

#$CGIQ       = new CGI;
#$ptTime     = localtime(time);
#$params     = &ftowa_toolkit::loadParameters();

# connect to the database
#$dbh = &ftowa_database::connect();
###

#$sessionID            = &ftowa_toolkit::isValidCookie( $dbh );
#$fields{"menu"}       = &ftowa_toolkit::getMenu();
#$fields{"logoncode"}  = &ftowa_toolkit::getMemberInfo( $sessionID );

#&ptHTML::displayHTML( $params{"page"}, %params );


